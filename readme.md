# What is MCP Sever 
# the model conext protocol (mcp) lets you build that expose data and functionality to LLM Application in a secure, standardized way. Think of it like a web Api but specifically designed for llm interaction. MCP server can :

expose data througth resources (think of those sort of like get endpoint; they are used to load information into the LLM's context )



# Core MCP Concepts
# MCP servers can provide three main types of capabilities:
# Resources: File-like data that can be read by clients (like API responses or file contents)
# Tools: Functions that can be called by the LLM (with user approval)
# Prompts: Pre-written templates that help users accomplish specific tasks




# MCP 3 Servers

# 1 Stdio
# 1 Streamable HTTP
# 3 Older SSE
